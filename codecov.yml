# Codecov Configuration for TI-Toolbox
# Documentation: https://docs.codecov.com/docs/codecov-yaml

# Coverage report settings
codecov:
  require_ci_to_pass: yes
  notify:
    wait_for_ci: yes

# Coverage targets and thresholds
coverage:
  precision: 2
  round: down
  range: "60...100"

  status:
    # Overall project coverage
    project:
      default:
        target: 70%
        threshold: 2%
        base: auto
        if_ci_failed: error

    # Coverage for individual patches/PRs
    patch:
      default:
        target: 65%
        threshold: 5%
        base: auto
        if_ci_failed: error

# Component-specific coverage flags
flags:
  unittests:
    paths:
      - tests/
    carryforward: false

  core:
    paths:
      - tit/paths.py
      - tit/constants.py
      - tit/calc.py
      - tit/logger.py
      - tit/errors.py
      - tit/config_io.py
    carryforward: false

  simulator:
    paths:
      - tit/sim/
    carryforward: false

  analyzer:
    paths:
      - tit/analyzer/
    carryforward: false

  optimizer:
    paths:
      - tit/opt/
    carryforward: false

  stats:
    paths:
      - tit/stats/
    carryforward: false

  plotting:
    paths:
      - tit/plotting/
    carryforward: false

  preprocessing:
    paths:
      - tit/pre/
    carryforward: false

  reporting:
    paths:
      - tit/reporting/
    carryforward: false

  atlas:
    paths:
      - tit/atlas/
    carryforward: false

# Files to ignore in coverage reports
ignore:
  - "**/__pycache__"
  - "**/*.pyc"
  - "**/test_*.py"
  - "tests/**"
  - "**/gui/**"        # GUI code requires X11/display environment
  - "**/__init__.py"
  - "scripts/**"
  - "tit/tools/**"     # Standalone scripts, not library code
  - "tit/blender/**"   # Blender-specific code

# Comment configuration for pull requests
comment:
  layout: "reach,diff,flags,tree,footer"
  behavior: default
  require_changes: false
  require_base: no
  require_head: yes
  branches:
    - main

# GitHub status checks configuration
github_checks:
  annotations: true
