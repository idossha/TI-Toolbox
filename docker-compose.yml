services:

  freesurfer:
    image: idossha/ti-toolbox_freesurfer:v7.4.1
    platform: linux/amd64
    container_name: freesurfer_container
    volumes:
      - ti-toolbox_freesurfer_data:/usr/local/freesurfer/
    networks:
      - ti_network
    restart: unless-stopped

  simnibs:
    platform: linux/amd64
    image: idossha/simnibs:v2.2.3
    container_name: simnibs_container
    volumes:
      - ti-toolbox_freesurfer_data:/usr/local/freesurfer/
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp/.X11-unix:/tmp/.X11-unix:rw      # X11 socket mount for Linux/macOS
      - ${LOCAL_PROJECT_DIR}:/mnt/${PROJECT_DIR_NAME}
      - ${HOME}/.Xauthority:/root/.Xauthority:rw       # Additional mount for macOS XQuartz compatibility
    environment:
      USER: root
      DISPLAY: ${DISPLAY}
      MPLBACKEND: "Agg"
      FREESURFER_HOME: /usr/local/freesurfer
      SUBJECTS_DIR: /usr/local/freesurfer/subjects
      FS_LICENSE: /usr/local/freesurfer/license.txt
      LOCAL_PROJECT_DIR: ${LOCAL_PROJECT_DIR}
      PROJECT_DIR_NAME: ${PROJECT_DIR_NAME}
      TZ: ${TZ}
      KMP_AFFINITY: "disabled"
      TI_TOOLBOX_VERSION: "v2.2.3"
    networks:
      - ti_network
    depends_on:
      - freesurfer
    tty: true
    stdin_open: true
    working_dir: /ti-toolbox
    restart: unless-stopped
    command: ["tail", "-f", "/dev/null"]

  qsiprep:
    image: pennlinc/qsiprep:latest
    platform: linux/amd64
    container_name: qsiprep_container
    profiles:
      - qsiprep
    entrypoint: ["/bin/bash", "-lc"]
    volumes:
      - ${LOCAL_PROJECT_DIR}:/data
      - ${LOCAL_PROJECT_DIR}:/out
      - ti-toolbox_freesurfer_data:/opt/freesurfer
      - ti-toolbox_qsiprep_data:/qsiprep-data
    environment:
      FS_LICENSE: /opt/freesurfer/license.txt
    networks:
      - ti_network
    tty: true
    stdin_open: true
    command: ["tail -f /dev/null"]

networks:
  ti_network:
    driver: bridge

volumes:
  ti-toolbox_freesurfer_data:
    external: true
    name: ti-toolbox_freesurfer_data
  ti-toolbox_qsiprep_data:
    external: true
    name: ti-toolbox_qsiprep_data

